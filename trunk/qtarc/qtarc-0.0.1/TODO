0.0.1:
	* Open
	* List
	* Add
	* Extract
* password
* New
* compression level, solid
* CLI
* DE integration (KDE, GNOME - *.desktop)
* Cut/Copy/Paste
* MoveTo, MoveFrom
* Drug'n'Drop
* link w/ natural libs
* arj, rpm, deb, iso, cab
= Or =
use mc (/usr/libexec/mc/ext.d)

= Current =
MainWindow->treeView->model(ArchItemModel(Archive))
ArchItemModel(QAbstractItemModel): Archive(archive) - model for viewing
Archive	- archive file representation (load,save,extract etc); root=ArchItem
ArchItem - a folder/file in archive
ArchItemPack	// Class containing ArchItems - as children of ArchItem

= Need =
misc arch handlers:
	- list
	- get info
	- add
	- del
	- extract
	- replace?
