pyqtarc-sql - branch to aprove sql cache for archive and filesystem
DB struct:
	* archive:
		* id:ID
		* parent:IDREF[NULL]
		* name:str
		* isdir:bool
		* datetime:datetime
		* size:long[NULL]
		* csize:long[NULL]
		? iname:str
		? path:str
		? ipath:str
	* fs:
		* path:str

pragma foreign_keys = on;
