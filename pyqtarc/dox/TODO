0.0.1:
	* List
	* Add
	* Extract
	* Del
	* treeview => listview
	* base handler class
	* handler capabilities
	* zip handler
	* auto handler selection
	* reload model after load
	* recurse extract (extract recursively)
	* check on exists (add) - replace/skip
	* wrap on resize
	- check on exists (extract)
	- check everything
	- cyr
	- setup
	- rpm
0.0.2:
	- Add: += Update
	- Drug'n'Drop
	- statusbar
	- tableview
	- add to archive subfolder ()
	- mkdir
	- rename
	- check archivers
0.0.3:
	* password
	* New
	* compression level, methods, solid, volumes
	* Cut/Copy/Paste
	* MoveTo, MoveFrom (?)
	* jar, arj, rpm, deb, iso, cab etc
	* DE integration (KDE, GNOME - *.desktop)
	* CLI (?)
0.0.4:
	* C++
0.0.X:
	* Open file in archive
= usual functions =
filter(func, iter) - пропускает iter через func
map - Apply function to every item of iterable and return a list of the results
reduce - Apply function of two arguments cumulatively to the items of iterable, from left to right, so as to reduce the iterable to a single value.
zip - 