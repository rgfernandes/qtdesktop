+ нет переводов
+ не обрабатывается изменение/удаление файла во время просмотра
+ не обрабатывается удаление папки с файлом
+ не обрабатывается отсутствие открытого файла
loadFile:
* не обрабатывается если файл не открылся
* прошита кодировка utf8
* не обрабатывается если это не plain text
* непонятно как будет работать с громадными файлами
* непроверена реакция на не-ascii filename
fileOpen:
* не проверяется - дадено имя файла или хрень несуразная
* не проверяется наличие файла
* не проверяется - это файл, папка, иноэ
* в фильтр прошито только *.txt
onActionFileOpenNext/onActionFileOpenPrev:
* не обрабатывается удаление/добавления файлов во время просмотра
* как и удаление папки с файлами вообще
